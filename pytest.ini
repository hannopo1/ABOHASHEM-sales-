[pytest]
# Where the unit tests actually live. Without this, bare `pytest` walks the
# whole repository and imports anything named like a test — including the
# browser-driven verification scripts under mobile/tools/, which need
# dependencies the CI image does not install.
testpaths =
    analysis/tests
    executive_dashboard/tests
